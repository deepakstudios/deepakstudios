# Secrets & local config — NEVER commit
config.php

# Runtime state
storage/

# Owner-uploaded gallery photos — NEVER commit (owner drops images on the server
# into photos/wedding/<letter>/; keep only the .gitkeep placeholders tracked)
photos/
!photos/wedding/*/
!photos/wedding/*/.gitkeep
!photos/prewedding/
!photos/prewedding/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
# Local PowerShell scratch scripts (grev_*.ps1 etc.) � never committed
*.ps1
